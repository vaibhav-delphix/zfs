[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
debug = true
overflow-checks = true
incremental = true

[workspace]
members = [
    "client",
    "server",
    "zettacache",
    "zettaobject",
    "zoa",
]
